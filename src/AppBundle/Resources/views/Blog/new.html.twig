{% extends 'AppBundle:Admin:base.html.twig' %}

{% block content -%}
    <div class="container">
    <h1>Blog creation</h1>

    {{ form(form) }}

        <p></p>
        <a href="{{ path('admin_blog') }}"  class="btn btn-default btn-primary" role="button">
            Back to the list
        </a>
    </div>
{% endblock %}
