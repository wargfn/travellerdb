{{ deck.name }}

{{ deck.faction.name }}
{#{% for agenda in deck.agendas %}
{{ agenda.card.name }}
{% endfor %}#}


{% set first_pack = deck.included_packs|first %}
{% set last_pack = deck.included_packs|last %}
Packs: {% if deck.included_packs|length > 1 %}
From {{ first_pack.pack.name }}{% if first_pack.nb > 1 %} ({{ first_pack.nb }}){% endif %} to {{ last_pack.pack.name }}{% if last_pack.nb > 1 %} ({{ last_pack.nb }}){% endif %}
{% else %}
{{ first_pack.pack.name }}{% if first_pack.nb > 1 %} ({{ first_pack.nb }}){% endif %}
{% endif %}


{% set adventures = deck.slots_by_type.adv %}
Adventure Card{% if adventures|length > 1 %}s{% endif %}

{% for slot in adventures %}
{{ slot.quantity }}x {{ slot.card.name }} ({{ slot.card.pack.name }})
{% endfor %}


{% set conns = deck.slots_by_type.conn %}
Connection Card{% if conns|length > 1 %}s{% endif %}

{% for slot in conns %}
{{ slot.quantity }}x {{ slot.card.name }} ({{ slot.card.pack.name }})
{% endfor %}

{% set crews = deck.slots_by_type.crew %}
Crew Card{% if crews|length > 1 %}s{% endif %}

{% for slot in crews %}
{{ slot.quantity }}x {{ slot.card.name }} ({{ slot.card.pack.name }})
{% endfor %}

{% set gears = deck.slots_by_type.gear %}
Gear Card{% if gears|length > 1 %}s{% endif %}

{% for slot in gears %}
{{ slot.quantity }}x {{ slot.card.name }} ({{ slot.card.pack.name }})
{% endfor %}

{% set events = deck.slots_by_type.event %}
Event{% if events|length > 1 %}s{% endif %}

{% for slot in events %}
{{ slot.quantity }}x {{ slot.card.name }} ({{ slot.card.pack.name }})
{% endfor %}

{%  set heroics = deck.slots_by_type.heroic %}
Heroic Event{%  if heroics|length > 1 %}s{%  endif %}

{% for slot in heroics %}
{{ slot.quantity }}x {{ slot.card.name }}  ({{  slot.card.pack.name }})
{% endfor %}

{%  set upgrades = deck.slots_by_type.upgrade %}
Upgrade{%  if upgrades|length > 1 %}s{%  endif %}

{% for slot in upgrades %}
{{ slot.quantity }}x {{ slot.card.name }}  ({{  slot.card.pack.name }})
{% endfor %}

{{ footer|default('') }}